# dashboard

Placeholder — not scaffolded yet.

When you're ready to start this:

```bash
cd apps
pnpm create next-app@latest dashboard --typescript --tailwind --eslint --app --src-dir=false --import-alias "@/*" --use-pnpm --yes
cd dashboard
npx shadcn@latest init
```
